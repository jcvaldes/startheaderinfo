organization: globalhitss

service: startheaderinfo

provider:
  name: aws
  runtime: nodejs20.x

plugins:
  - serverless-offline

functions:
  hello:
    handler: handler.hello
    events:
      - httpApi: "*"